version: 0.0
os: linux
files:
  - source: /
    destination: /www/wwwroot/conect.app
post-update:
  - cd /www/wwwroot/conect.app && chown -R www-data:www-data . && chmod -R 775 . && chmod -R 777 writable && sudo -u www-data composer update --no-dev
